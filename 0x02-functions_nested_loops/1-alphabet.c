#include "main.h"

/**
 * main - entry point
 *
 * Return: 0 - success
 */

void print_alphabet(void)
{
	char = a
	while (a < 'a'; a <= 'z'; a++);
        {
	_putchar(c); }
	_putchar('\n');
        return (0);
}

