#include "main.h"

/**
 * main - entry point
 *
 * islower: function prints the 1 if conditions arre met
 *
 * Return: 0 - success
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1); }
	else
	{
	return (0); }
}
