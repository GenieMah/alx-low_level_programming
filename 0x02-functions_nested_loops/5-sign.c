#include "main.h"

/**
 * main - entry point
 *
 * print_alphabet function prints the alphabet
 *
 * Return: 0 - success
 */

int print_sign(int n)
{
	if (n = 0) 
	{return 0
		_putchar('0')}
	else if (n > 0)
	{return 1
		_putchar('+')}
	else if (n < 0)
	{return -1
		_putchar('-')}
}
