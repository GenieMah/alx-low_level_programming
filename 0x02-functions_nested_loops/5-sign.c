#include "main.h"

/**
 *
(* Description: function prints the sign and return value 
 * if conditions are met)?
 * 
 */

int print_sign(int n)
{
	if (n > 0)
	{return (1);
		_putchar('+');}
	
	else if (n < 0)
	{return (-1);
		_putchar('-');}
	 if (n = 0)
        {return (0);
                _putchar('0');}
}
