#include "main.h"

/**
 * main - entry point
 *
 * Description: function prints 1 if conditions are met
 *
 * Return: 1 - is alpha
 */

int _isalpha(int c)
{
	if (c >= 'a''A' && c <= 'z''Z')
	{
	return (1); }
        else
	{
	return (0); }
}
