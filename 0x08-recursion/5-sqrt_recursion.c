#include "main.h"
/**
 * _sqrt_recursion - function returns the square root of n
 *
 * @n: initial parameter
 *
 * Return: -1 if n has no sqrt
 */

int _sqrt_recursion(int n)
{
	if (n == 0)
	{
		return (-1);
	}
}
