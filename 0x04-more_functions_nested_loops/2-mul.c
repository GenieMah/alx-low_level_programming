#include "main.h"

/**
 * mul - function that prints out the multiplication of two values
 *
 * @a: initial parameter
 * @b: secondary parameter
 *
 * Return: the multiplication of the two values from a and b
 */

int mul(int a, int b)

{
	return (a * b);
}
