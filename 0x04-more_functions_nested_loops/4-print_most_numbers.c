#include "main.h"

/**
 *
 * Description: function prints out numbers but 2 and 4
 *
 */

void print_numbers(void)
{
	char a = 0;
		while (a = '0'; a<= '9')
	{
		if (a != 2 && a != 4)
		_putchar('0' + a);
			a++;}
	_putchar('\n');
}
