#include "main.h"
/**
 *
(* Description - function that returns 1 when a digiit is
 * detected and 0 otherwise)?
 *
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1); }

	else
	{
	return (0); }
}
