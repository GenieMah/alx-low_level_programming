#include <stdio.h>

/**
 * main - entry point
 *
(* Description - script that prints a-z in lowercase
 * then uppercase)?
 *
 * Return: 0 - Success
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; ++a)

	printf("%d", a);

	printf("\n");

		return (0);
}
