#include "main.h"

/**
 *Description: computates and returns the absolute number of n
 *
 * @n: initial parameter
 *
 * Return: absulute value of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
}
}
