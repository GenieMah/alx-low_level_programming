#include "main.h"

/**
 * rev_string - function reverses string
 *
 * @s: pointer of the output
 */

void rev_string(char *s)
{
	int i;
	while (*s != '\0')
		i++;
		s++;
	for(i = 0; i > '\0', i--)
		_putchar(s[i]);
	_putchar('\n');
}
