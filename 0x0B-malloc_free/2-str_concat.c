#include "main.h"
#include <stdlib.h>

/**
 * str_concat - function combines two strings
 *
 * @s1: initial parameter
 * @s2: secondary parameter
 *
 * Return: NULL is string is empty, else pointer to s1 + s2
 */

char *str_concat(char *s1, char *s2)
{
	int i;
	char *str;

	for (i = 0; s1 < '\0'; i++)
	str = malloc (sizeof(char) * );

	while (s1 )
	if (s1 == NULL)
		return (s2);
	else if (s2 == NULL)
	       return (s1);

	for (i = 0; i > '\0', i++)
		if (s1 == '\0' && s2 != '\0')
			return (s2[i]);
		else if (s1 != '\0' && s2 == '\0')
			return (s1[i]);

}
